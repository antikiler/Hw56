{% extends "base.html.twig" %}

{% block title %}Login page{% endblock %}

{% block body %}
    <div class="alert-warning">{{ error }}</div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('app_basic_register') }}">Register</a>
{% endblock %}
